/**
 * Registro de render JSON → componentes MUI (componente de componentes TK).
 * Cada `kind` en TK_CONTENT se interpreta aquí; la BD solo almacena JSON anidado.
 */
export { BlockBody as TkDocBlockRenderer, BlockBody } from "./tkDoc/BlockBody.jsx";
